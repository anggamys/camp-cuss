export enum OrderStatus {
  pending = 'pending',
  canceled = 'canceled',
  accepted = 'accepted',
  completed = 'completed',
}

export enum OrderService {
  ride = 'RIDE',
  delivery = 'DELIVERY',
  food = 'FOOD',
}
